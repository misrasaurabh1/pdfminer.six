[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pdfminer-core"
version = "0.1.0"
description = "Rust extensions for pdfminer.six"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "pdfminer_core"
